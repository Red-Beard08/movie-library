{
  "name": "movie-library",
  "version": "1.0.0",
  "description": "A Markdown-first movie and series library for Obsidian.",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "typecheck": "tsc --noEmit",
    "test:utils": "node --test tests/utils.test.mjs",
    "test": "npm run typecheck && npm run test:utils && npm run build"
  },
  "author": "Red-Beard",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "typescript": "^5.7.0"
  }
}
